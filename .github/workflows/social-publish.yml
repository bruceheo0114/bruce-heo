name: Publish due social content

on:
  schedule:
    # Every day 18:30 Asia/Seoul (GitHub cron uses UTC)
    - cron: "30 9 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: Validate the next item without publishing
        required: false
        type: boolean
        default: false

concurrency:
  group: bruce-content-automation
  cancel-in-progress: false

permissions:
  contents: write
  issues: write

jobs:
  publish-one:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GH_TOKEN: ${{ github.token }}
      OPENAI_MODEL: gpt-5.6-terra
      PUBLIC_SITE_URL: ${{ vars.PUBLIC_SITE_URL || 'https://bruceheo0114.github.io/bruce-heo' }}
      LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
      LINKEDIN_PERSON_URN: ${{ secrets.LINKEDIN_PERSON_URN }}
      LINKEDIN_API_VERSION: ${{ vars.LINKEDIN_API_VERSION || '202605' }}
      IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
      IG_USER_ID: ${{ secrets.IG_USER_ID }}
      META_GRAPH_VERSION: ${{ vars.META_GRAPH_VERSION || 'v25.0' }}
      IG_COLLABORATOR_USERNAME: ${{ vars.IG_COLLABORATOR_USERNAME || 'heo.boram' }}
      IG_TAG_USERNAME: ${{ vars.IG_TAG_USERNAME || 'heo.boram' }}
      SOCIAL_DRY_RUN: ${{ inputs.dry_run || 'false' }}
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install and validate
        run: |
          corepack enable
          pnpm install --frozen-lockfile
          pnpm test
          pnpm check

      - name: Publish at most one due package
        id: publish
        continue-on-error: true
        run: pnpm publish:due

      - name: Persist channel results
        if: always() && steps.publish.outcome != 'skipped'
        run: |
          git config user.name "bruce-content-bot"
          git config user.email "bruce-content-bot@users.noreply.github.com"
          git add -- data/automation-state.json
          while IFS= read -r file; do
            [ -n "$file" ] && git add -- "$file"
          done < <(git diff --name-only -- content)
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "chore: persist social publishing state"
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Report publishing failure
        if: steps.publish.outcome == 'failure'
        run: |
          title="[자동화] SNS 게시 실패"
          body=$(printf '실행: https://github.com/%s/actions/runs/%s\n\n성공한 채널 ID는 저장했으며 실패한 채널만 다음 슬롯에 재시도합니다. 토큰 만료, 앱 권한, 공개 이미지 URL을 확인해 주세요.' "$GITHUB_REPOSITORY" "$GITHUB_RUN_ID")
          issue=$(gh issue list --state open --search "$title in:title" --json number --jq '.[0].number // empty')
          if [ -n "$issue" ]; then
            gh issue comment "$issue" --body "$body"
          else
            gh issue create --title "$title" --body "$body"
          fi
          exit 1
